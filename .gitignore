bin/
obj/
*.user
.DS_Store

# Local machine overrides (Valheim path etc.)
Directory.Build.props.user

# Build output staged for Thunderstore
thunderstore/build/

# .claude/ is local working state (plans, handoffs, research), except the design
# rationale CLAUDE.md points at. `.claude/*` rather than `.claude/` so git still
# looks inside far enough to see the exception.
.claude/*
!.claude/design-decisions.md

# Automatic test output (scripts/autotest.sh)
.devtest/
